import React from 'react';
import './ChatNotificationBadge.css';

const ChatNotificationBadge = ({ userId, onCountChange }) => {
  // Badge disabled - too unreliable with Firestore sync
  return null;
};

export default ChatNotificationBadge;

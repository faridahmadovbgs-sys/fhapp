# Production Environment Variables Template
# Copy this to .env.production and fill in your values

# Application Environment
NODE_ENV=production
PORT=5000

# Frontend URL (will be updated after Vercel deployment)
CLIENT_URL=https://your-app-name.vercel.app

# Firebase Configuration (already configured in client)
# No additional server-side Firebase config needed

# Security
CORS_ORIGIN=https://your-app-name.vercel.app

# Initial Admin Setup (optional - remove after first admin is created)
# INITIAL_ADMIN_EMAIL=your-admin@domain.com

# API Configuration
API_VERSION=v1

# Logging (optional)
LOG_LEVEL=info